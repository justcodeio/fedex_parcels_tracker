require "spec_helper"

describe FedexParcelsTracker do
  it "has a version number" do
    expect(FedexParcelsTracker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
